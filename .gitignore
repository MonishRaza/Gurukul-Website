# Working folder for personal files before they are added to the site —
# keep it out of the public repository
my-content/

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
# Node deps + generated QA contact sheets for the photo pipeline (_tools)
_tools/node_modules/
_tools/sheets/
_tools/qa-*.jpg
_tools/albums-block.txt
